# PIIcasso Backend — Enterprise Dependencies
# ============================================
# Cleaned: removed unused packages

# Core framework
Django==5.2.16  # 5.2 LTS security release; intentionally not upgraded to Django 6
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1  # security: CVE-2024-22513
django-cors-headers==4.9.0

# API documentation
drf-spectacular==0.29.0

# Database
dj-database-url==3.1.2
psycopg2-binary>=2.9.9,<3

# Caching & sessions
django-redis==5.4.0
redis>=8.0.0,<9

# Static files & storage
whitenoise==6.8.2

# Security & auth
PyJWT==2.13.0  # security: PYSEC-2026-120/175/177/178/179, PYSEC-2025-183 (2.13.0 closes the full SNYK pyjwt set)
cryptography==49.0.0
# django-cryptography was unused (only `cryptography.fernet.Fernet` is imported
# directly by generator/fields.py) and the upstream package is unmaintained
# since 2023 — dropping it removes a stale, vulnerable transitive surface.

# AI / LLM (Google Gemini)
# google-generativeai is deprecated; google-genai is the supported unified SDK.
google-auth==2.56.2
google-genai==2.14.0
pydantic==2.13.4

# HTTP clients
requests==2.33.0  # security: CVE-2024-47081, CVE-2026-25645
# security: floor raised to 2.7.0 — <2.7.0 is vulnerable to data-amplification
# (SNYK-PYTHON-URLLIB3 highly-compressed-data) and sensitive-info leakage on redirect
urllib3>=2.7.0,<3

# PDF / Report generation
reportlab==4.2.5

# Image processing (transitive use only — ReportLab image embedding)
pillow==12.3.0

# Environment
python-dotenv==1.2.2  # security: CVE-2026-28684

# Error tracking
sentry-sdk[django]==2.61.1

# Utilities
PyYAML==6.0.2
defusedxml==0.7.1
django-prometheus==2.3.1

# Async & Real-time Processing (Added for Enterprise Scale)
celery==5.4.0
channels==4.2.2
channels-redis==4.3.0
daphne==4.2.2

# Transitive deps of the daphne/channels websocket stack, pinned to patched
# releases for security. Not imported directly by application code.
Twisted>=26.4.0  # security: PYSEC-2026-160
# Note: twisted still carries one un-patched advisory (SNYK-PYTHON-TWISTED-12671201,
# "Arbitrary Command Injection", medium) with no fixed release as of 2026-06; not
# reachable from this app's code paths — accepted until upstream ships a fix.
pyOpenSSL==26.3.0

# Vulnerable transitive deps pinned to patched floors. Not imported directly by
# application code; without these the resolver pulls older, flagged versions.
click>=8.4.1      # security: SNYK-PYTHON-CLICK-16347201 (command injection) — via celery
idna>=3.15        # security: SNYK-PYTHON-IDNA-16769942 (ReDoS) — via requests
httplib2>=0.32.0  # security: PYSEC-2026-3444 — via Google auth tooling
msgpack>=1.2.1    # security: CVE-2026-57585 — via distributed tooling
pyasn1>=0.6.4     # security: 2026 parser advisories — via google-auth
sqlparse>=0.5.4   # security: SNYK-PYTHON-SQLPARSE-14157217 — via django
ujson>=5.13.0     # security: PYSEC-2026-2294 — via daphne -> autobahn
zipp>=3.19.1      # security: SNYK-PYTHON-ZIPP-7430899 — via importlib-metadata
